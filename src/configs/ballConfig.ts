import { Vector3 } from "@babylonjs/core";

// ball size for game level
export const ballsSize: number[] = [0.5, 0.4, 0.3];

// ball mass for game level
export const ballsMass: number[] = [1, 1, 1];

// multiplier balls for the level of the game
export const ballLevelMultiplier: number[] = [5, 5, 5];

// ball position deviation
export const positionDeviation: Vector3 = new Vector3(0.2, 0.2, 0.2);

