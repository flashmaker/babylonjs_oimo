import { Vector3 } from "@babylonjs/core";

// multiplier for game
export const gateMultipliers: number[] = [3, 4, 5, 6];

// gate's position
// TODO add gate's position
export const gatePositions: Array<Vector3> = [];